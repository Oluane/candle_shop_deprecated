// import { loadCartStateFromLocalStorage, saveCartStateToLocalStorage } from "../localStorageUtils";

// // test("saving cart state to localstorage"), () => {
// //     expect(localStorage)
// // }

// describe("working with cart state & localStorage", () => {
// 	test("saving cart to localStorage", () => {
// 		const cartState = {
// 			cart: {
// 				products: [{ candleId: 2 }],
// 				totalCost: 140,
// 			},
// 		};
// 		saveCartStateToLocalStorage(cartState);
// 		//expect(localStorage.getItem.mock.calls.length).toBeGreaterThan(0);

// 		expect(localStorage.setItem).toBeCalledTimes(1);
// 	});
// });
